# Correlator Environment Variables
# Copy this file to .env and update with your values
# The .env file is automatically loaded by docker-compose

#===============================================================================
# Database Configuration
#===============================================================================
POSTGRES_PASSWORD=dev123

#===============================================================================
# Correlator API Configuration
#===============================================================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Enable API key authentication (set to true for production)
CORRELATOR_AUTH_ENABLED=false

# Path to namespace alias configuration (inside container)
CORRELATOR_CONFIG_PATH=/config/.correlator.yaml

#===============================================================================
# Web UI Configuration
#===============================================================================
# API URL for the frontend (use localhost for local dev)
NEXT_PUBLIC_CORRELATOR_URL=http://localhost:8080

#===============================================================================
# Build Information (set by CI/CD, optional for local dev)
#===============================================================================
VERSION=0.1.0-dev
GIT_COMMIT=unknown
BUILD_TIME=unknown

#===============================================================================
# Notes
#===============================================================================
# - Use strong passwords for production deployments
# - Never commit the actual .env file to version control
# - The .env file is in .gitignore
#
# Production example:
#   POSTGRES_PASSWORD=your-secure-password-here
#   CORRELATOR_AUTH_ENABLED=true
#   LOG_LEVEL=warn
