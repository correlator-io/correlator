# Correlator Environment Variables
# Copy this file to .env and update with your values
# The .env file is automatically loaded by docker-compose

# Database Configuration
POSTGRES_PASSWORD=password

# Migrator Configuration
DATABASE_URL=postgres://correlator:password@host.docker.internal:5432/correlator?sslmode=disable# pragma: allowlist secret

# Development Notes:
# - Use a strong password for production deployments
# - For local development, the override file defaults to 'dev123' if not set
# - Never commit the actual .env file to version control
# - The .env file should be added to .gitignore

# Example production values:
# POSTGRES_PASSWORD=Corr3l4t0r_Pr0d_P4ssw0rd_2024!

# Example development values:
# POSTGRES_PASSWORD=dev123
