# Correlator Demo Environment Variables
# Copy this file to .env and customize as needed
#
# Note: Database credentials use the same values as development:
#   User: correlator
#   Password: correlator_dev_password
# These are hardcoded in docker-compose.demo.yml for simplicity.

# =============================================================================
# Correlator
# =============================================================================
LOG_LEVEL=debug

# =============================================================================
# Airflow
# =============================================================================
# Generate a Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=

# Secret key for Airflow webserver sessions
# In production, generate a secure random key
AIRFLOW_SECRET_KEY=demo-secret-key-change-in-production
